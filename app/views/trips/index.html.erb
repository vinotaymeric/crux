<br>
<div class="container">
  <div class="row">

    <% @trips.each do |trip| %>
    <div class="col s12 s6 m4">
    <div class="card horizontal">
      <div class="card-image">
        <img src="https://res-1.cloudinary.com/deesjjv4h/image/upload/v1/crux/images/escalade.png.png">
      </div>
        <div class="card-stacked">
          <div class="card-content">
            <p class="title-card">Voyage du <%= trip.start_date %> depuis <%= trip.location.split(",")[0] %> </p>
          </div>
          <div class="card-action">
            <% if trip.validated %>
            <%= link_to 'Voir les itinéraires', trip_basecamps_activity_path(trip, BasecampsActivity.find(5843)), class: 'waves-effect waves-light btn-small' %>
            <% else %>
              <%= link_to 'Choisir le camp', trip_basecamps_activities_path(trip), class: "waves-effect waves-light btn-small" %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
    <% end %>

    </div>
  </div>
</div>
