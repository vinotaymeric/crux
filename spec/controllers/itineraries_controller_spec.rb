require 'rails_helper'

RSpec.describe ItinerariesController, type: :controller do
  it "is pending"

  # describe "GET index" do
  #   let!(:current_user) { login_user(create(:user)) }
  #   it "has a 200 status code" do
  #     @user = create(:user)
  #     get :index
  #     expect(response.status).to eq(200)
  #   end
  # end
end
